{{- if .Values.enabled }}

✅ infrasight-ml-syscall-per-container has been deployed.

Environment configuration:
  - KAFKA_BROKER  = {{ default (printf "%s-kafka:9092" .Release.Name) .Values.env.KAFKA_BROKER }}
  - KAFKA_TOPIC   = {{ .Values.env.KAFKA_TOPIC }}

To check logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "infrasight-ml-syscall-per-container.name" . }}

{{- else }}

⚠️ infrasight-ml-syscall-per-container is disabled (`enable=false`), so nothing was deployed.

{{- end }}

