{{- if .Values.enabled }}

 infrasight-gpu-fingerprint has been deployed.

Environment configuration:
  - KAFKA_BROKER         = {{ default (printf "%s-kafka:9092" .Release.Name) .Values.env.KAFKA_BROKER }}
  - KAFKA_TOPIC          = {{ .Values.env.KAFKA_TOPIC }}
To check logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "infrasight-gpu-fingerprint.name" . }}

{{- else }}

 infrasight-gpu-fingerprint is disabled (`enable=false`), so nothing was deployed.

{{- end }}

